name: Example

on:
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: self
    steps:
      - uses: actions/checkout@v3

      # - name: Set up Go
      #   uses: actions/setup-go@v4
      #   with:
      #     go-version: "1.20"
      # - name: Build
      #   run: go build -v ./...
      # - name: Test
      #   run: go test -v ./...

      - name: Run script file
        run: |
          echo "hello-world"
          pwd
        shell: bash
